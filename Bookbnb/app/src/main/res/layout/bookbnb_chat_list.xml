<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View"/>
        <variable name="chatsViewModel" type="com.fiuba.bookbnb.ui.fragments.chat.ChatsViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@style/bkbnbNormalTitle"
            android:paddingHorizontal="@dimen/bkbnb24dp"
            android:paddingTop="@dimen/bkbnb32dp"
            android:paddingBottom="@dimen/bkbnb16dp"
            android:text="@string/footer_bar_messages_menu" />

        <View
            android:id="@+id/header_divider"
            android:layout_width="match_parent"
            android:layout_height="@dimen/bkbnb2dp"
            android:background="@color/colorBackgroundInputFieldDisabled"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/tabs_content" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/chats_container"
            android:name="com.fiuba.bookbnb.ui.fragments.chat.ChatsFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingBottom="@dimen/bkbnb8dp"
            android:clipToPadding="false"
            app:layoutManager="LinearLayoutManager"
            app:layout_constrainedHeight="true"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/empty_chats_text"
            tools:listitem="@layout/bookbnb_chat_item" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/empty_chats_text"
            style="@style/bkbnbOptionProfileMenu"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:layout_margin="@dimen/bkbnb22dp"
            android:visibility="@{chatsViewModel.chats.size() == 0 ? View.VISIBLE : View.GONE}"
            android:text="@string/chats_empty"/>
    </LinearLayout>

</layout>
